#ifndef DEV_STANDARD_H
#define DEV_STANDARD_H
#endif // !DEV_STANDARD_H

#include <stdlib.h>
#include <stdio.h>
#include <math.h>

extern double calcola_dev(const double *vec, size_t size);
extern double calcola_med(const double *vec, size_t size);