#include "rimuovi.h"

void rimuovi_stringa(char *parola, const char *str)
{
	/*if (str == NULL)
		return;
	if (strcmp("", str))
		return;
	*/
	
	for (size_t i = 0; i < strlen(parola); i++)
	{
		if(parola[i])
	}
	
}