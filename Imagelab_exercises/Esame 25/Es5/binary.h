#ifndef BINARY_H
#define BINARY_H
#endif // !BINARY_H

#include <stdio.h>
#include <stdlib.h>

extern void stampa_binario(const char* filename_in, const char* filename_out);
extern char *decimal_to_binary(int n);